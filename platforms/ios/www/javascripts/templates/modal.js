define(["jade"],function(jade){

return function template(locals) {
var buf = [];
var jade_mixins = {};
var jade_interp;

buf.push("<div class=\"modal-backdrop active\"></div><div class=\"modal\"><div class=\"padding\">Replace with yours</div></div>");;return buf.join("");
};

});
